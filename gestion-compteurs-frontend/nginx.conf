server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # React SPA - toutes les routes non-fichier redirigees vers index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
